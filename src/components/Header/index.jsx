import styles from "./Header.module.css";

function Header() {
    return (
        <header className={styles.header}>
            <div>
                <span>Fidelização Base</span>
            </div>
        </header>
    );
}

export default Header;
